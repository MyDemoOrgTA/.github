name: Deploy App

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Load secrets & Deploy
    uses: MyDemoOrgTA/.github/.github/workflows/secrets-poc-vault.yml@main
    secrets: inherit
